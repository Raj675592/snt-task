specVersion: "v1"
serviceId: "create-react-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "create-react-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
